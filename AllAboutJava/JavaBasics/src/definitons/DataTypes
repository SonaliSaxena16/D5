-DataTypes can be termed as a Container i.e. used to store the value
-A DataType is declared on the basis of values, which we want to store
--If we are expecting DataType int and we've passed String, then it would give ---------exception
-DataTypes are of two types -->
-Primitive --> Byte1 Short2 int4 long8 float4 double8 boolean char2
-Non Primitive --> class, enum, interface, String 