the Collection framework didn't include in Java. In the early version of Java, 
All the legacy classes are synchronized. The java.util package defines the following legacy classes:
in Java 1.0 version.

HashTable
Stack
Dictionary
Properties
Vector

A separate framework named the “Collection Framework” has been defined in JDK 1.2 which holds all the Java 
Collection Classes and Interface in it.
